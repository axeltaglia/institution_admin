
<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h2>Recibo virtual</h2>
    <p>
      <b>BeBop Escuela de Música</b> informa que se ha recibido el pago referido a las clases de <i><%= @fee.student.full_name %></i> por el mes de <b><%= @str_month  %></b>
    </p>

    <h3>Detalle</h3>
    <p>
      <% @fee.items.each do |item| %>
        <b>Clases de <%= item.asignature_name %></b> - <%= item.day %> de <%= item.start_at %> a <%= item.end_at %> - $<%= item.subscription_price %><br>
      <% end %>
      <b>TOTAL: </b> $<%= @fee.total_to_pay %> 
    </p>

    <p>
      Atentamente, <br> 
      BeBop Escuela de Música
    </p>
    <p><i>Sistema automático de Administración de Instituciones</i></p>
  </body>
</html>
