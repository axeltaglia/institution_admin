<div class="row">
  <div class="col-lg-12">
    <div class="panel panel-default">

      <div class="panel-heading">
      <h3 class="box-title"><%= t('owner.asignatures.index.title') %></h3>
      </div>

      <div class="panel-body">
        <table class="table table-bordered table-striped">
          <thead>
            <tr>
              <th><%= t('activerecord.attributes.asignatures.name') %></th>
              <th><%= t('form.column.actions') %></th>
            </tr>
          </thead>

          <tbody>
            <% @asignatures.each do |asignature| %>
            <tr>
              <td> 
                <%= link_to owner_asignature_path(asignature) do %> 
                <%= asignature.name %> 
                <% end %>
              </td>
              <td>
                <div class="btn-row">
                  <%= link_to raw('<i class="fa fa-eye"></i>'), owner_asignature_path(asignature), class: 'btn btn-sm btn-success' %>
                  <%= link_to raw('<i class="fa fa-pencil"></i>'), edit_owner_asignature_path(asignature), class: 'btn btn-sm btn-warning' %>
                  <%= link_to raw('<i class="fa fa-trash"></i>'), owner_asignature_path(asignature), class: 'btn btn-sm btn-danger', method: :delete,
                  data: { confirm: t('owner.asignatures.destroy.warning') } %>
                </div>
              </td>
            </tr>
            <% end %>
          </tbody>

          <tfoot>
            <tr role="row">
              <th><%= t('activerecord.attributes.asignatures.name') %></th>
              <th><%= t('form.column.actions') %></th>
            </tr>
          </tfoot>
        </table>
      </div>
    </div>
  </div>
</div>

<div class="row">
  <div class="col-md-2 col-md-offset-10">
    <%= link_to t('button.new'), new_owner_asignature_path, class: 'btn btn-success pull-right' %>
  </div>
</div>
