<%= form_for institution, url: url, role: :form, html: { multipart: true } do |f| %>
  <% if institution.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(institution.errors.count, "error") %> prohibited this institution from being saved:</h2>

      <ul>
      <% institution.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <% f.label :institution_owner_id %>
    <%= collection_select(:institution, :institution_owner_id, InstitutionOwner.all, :id, :email, :include_blank => true) %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
